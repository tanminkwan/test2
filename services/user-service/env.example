# 서버 설정
NODE_ENV=development
PORT=3002

# 데이터베이스 설정
# 지원되는 타입: memory, sqlite, postgres, mysql
DB_TYPE=memory

# 메모리 데이터베이스 (기본값) - 외부 의존성 없음
# 재시작 시 데이터 초기화됨

# SQLite 파일 데이터베이스 설정 (DB_TYPE=sqlite일 때)
# DB_PATH=./data/user_service.db

# PostgreSQL 설정 (DB_TYPE=postgres일 때)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=user_service
# DB_USER=user
# DB_PASS=password

# MySQL 설정 (DB_TYPE=mysql일 때)
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=user_service
# DB_USER=user
# DB_PASS=password

# JWT 설정
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRY=24h

# 프록시 설정 (필요한 경우)
HTTP_PROXY=http://70.10.15.10:8080
HTTPS_PROXY=http://70.10.15.10:8080
NO_PROXY=localhost,127.0.0.1,::1 